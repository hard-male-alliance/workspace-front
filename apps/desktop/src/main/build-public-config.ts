/** @file 构建时嵌入的桌面公开配置 / Public desktop configuration embedded at build time. */

/** @brief 由 electron-vite 在构建时静态替换的 native public client ID / Native public-client ID statically replaced by electron-vite at build time. */
export const DESKTOP_BUILD_OAUTH_CLIENT_ID = __AI_JOB_WORKSPACE_OAUTH_CLIENT_ID__
