/** @file Identity 限界上下文公共入口 / Identity bounded-context public entry. */

export type { IdentityGateway } from './application/gateway'
export type { UiCurrentUser, UiCurrentUserId } from './domain/models'
